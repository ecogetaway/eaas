{
  "buildCommand": "cd eaas-frontend && npm install && npm run build",
  "outputDirectory": "eaas-frontend/dist",
  "installCommand": "cd eaas-frontend && npm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}

